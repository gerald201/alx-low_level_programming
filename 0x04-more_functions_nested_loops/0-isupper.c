#include "main.h"
/**
* _isupper - checks for uppercase character
* @c: Variable text
* Return: 1 for upper case or 0 anything else
*/
int _isupper(int c)
{

if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}

}
